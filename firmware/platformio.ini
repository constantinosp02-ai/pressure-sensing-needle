; TODO: PlatformIO toolchain file is not committed yet.
; The final build used the Arduino IDE on ESP8266 (NodeMCU 1.0, ESP-12E module).
; When the firmware is added to firmware/src/, replace this file with the real
; platformio.ini, or remove it if the build stays on the Arduino IDE.
;
; Reference: Section 4.4.1, Appendix A.6 (Table 33).
